[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
pytest = "*"
trio = ">=0.13.0"
curio = "*"
pytest-cov = "*"

[packages]
anyio = ">=1.2.0"
pyudev = {version = ">=0.21.0",platform_system = "=='Linux'"}
pyusb = ">=1.0.2"
async-generator = ">=1.10"

[requires]
python_version = "3.7"

[scripts]
test = "pytest --anyio-backends=all"
coverage = "pytest --anyio-backends=all --cov=src --cov-report html --cov-report term test/"
